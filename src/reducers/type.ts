export type stateType = {
    tab: string,
    data: string[]
}

export type actionType={
    type: string,
    tab: string,
    data: string[]
}
export type objectType={
    id: number,
    title: string,
    body: string,
    name:string
}